apiVersion: apps/v1
kind: Deployment
metadata:
  name: django-green
  namespace: default
  labels:
    app: django
    slot: green
    env: production
    version: GIT_SHA
spec:
  replicas: 1
  selector:
    matchLabels:
      app: django
      slot: green
  template:
    metadata:
      labels:
        app: django
        slot: green
        env: production
        version: GIT_SHA
    spec:
      serviceAccountName: django-ksa
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
      initContainers:
        - name: db-migrate
          # SUBSTITUTE: Replace PROJECT_ID and GIT_SHA before applying.
          image: us-central1-docker.pkg.dev/PROJECT_ID/talana-artifact-registry/django:GIT_SHA
          command: ["python", "manage.py", "migrate", "--noinput"]
          env:
            - name: DJANGO_SETTINGS_MODULE
              value: "talana.settings"
            - name: GCP_PROJECT_ID
              value: "PROJECT_ID"
            - name: DJANGO_ALLOWED_HOSTS
              # Domain-only is safe: smoke-test curls ClusterIP via GET /healthz/ which
              # never triggers Django's ALLOWED_HOSTS check (no middleware calls
              # request.get_host() with SECURE_SSL_REDIRECT=False + PREPEND_WWW=False).
              # K8s liveness/readiness probes (httpGet /healthz/) follow the same path.
              # If new POST endpoints or middleware that calls get_host() are added,
              # ClusterIP/pod-IP traffic must also be added here.
              value: "talana.nacholar.com"
            - name: DJANGO_SECURE_SSL_REDIRECT
              value: "False"
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
            allowPrivilegeEscalation: false
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
      containers:
        - name: django
          # SUBSTITUTE: Replace PROJECT_ID and GIT_SHA before applying.
          image: us-central1-docker.pkg.dev/PROJECT_ID/talana-artifact-registry/django:GIT_SHA
          ports:
            - containerPort: 8000
          env:
            - name: DJANGO_SETTINGS_MODULE
              value: "talana.settings"
            - name: GCP_PROJECT_ID
              value: "PROJECT_ID"
            - name: DJANGO_ALLOWED_HOSTS
              # Domain-only is safe: smoke-test curls ClusterIP via GET /healthz/ which
              # never triggers Django's ALLOWED_HOSTS check (no middleware calls
              # request.get_host() with SECURE_SSL_REDIRECT=False + PREPEND_WWW=False).
              # K8s liveness/readiness probes (httpGet /healthz/) follow the same path.
              # If new POST endpoints or middleware that calls get_host() are added,
              # ClusterIP/pod-IP traffic must also be added here.
              value: "talana.nacholar.com"
            - name: DJANGO_SECURE_SSL_REDIRECT
              value: "False"
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
            allowPrivilegeEscalation: false
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /healthz/
              port: 8000
              httpHeaders:
                - name: Host
                  value: talana.nacholar.com
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz/
              port: 8000
              httpHeaders:
                - name: Host
                  value: talana.nacholar.com
            initialDelaySeconds: 10
            periodSeconds: 5
