apiVersion: v1
kind: ServiceAccount
metadata:
  name: django-ksa
  namespace: default
  annotations:
    # The WI binding (default/django-ksa → talana-app-sa) is provisioned by Terraform (story 1.4).
    iam.gke.io/gcp-service-account: talana-app-sa@talana-491221.iam.gserviceaccount.com
